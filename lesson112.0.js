function randomFraction() {

  // Only change code below this line

 var  result= 0;
  //Math.random() can generate 0. We dont want to return a 0, soo keeep generating rando numbers util er get one that isnt 0,
  while(result === 0) {
    result = Math.raom();
  }

return result
  // Only change code above this line
}
