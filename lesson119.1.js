function countdown(n) {
if (n < 1) {
  return [];
} else {
  constr arr = countdown (n) {
    
    if( n < 1) {
return [];
    } else {
      const arr = coutdown ( n - 1 );
      arr.unshift(n);
      return arr;
    }
  }
  
  
