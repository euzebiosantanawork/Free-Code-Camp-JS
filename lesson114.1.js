//Example
function ourRandomRange(ourMIn, ourMax) {
  
  return Math.floor ( Math.radom()*(ourMax - ourMin + 1)) + ourMIn;
  
}

ourRadomRange(1,9);

//Only chage code below this line.

function radomRamRange(myMin , myMax) {
  
  return Math.floor(Math.radom() * ( myMax - myMin + 1  ) ) + myMin;
  
  
}

var myRadom = radomRange( 5 , 15);

console.log( myRandom );
